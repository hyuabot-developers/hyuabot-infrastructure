# Persistent Volume
apiVersion: v1
kind: PersistentVolume
metadata:
  name: postgres-pv-volume
  namespace: hyuabot
spec:
  capacity:
    storage: 5Gi
  volumeMode: Filesystem
  accessModes:
  - ReadWriteOnce
  hostPath:
    path: /mnt/data/postgres-pv-volume
  storageClassName : postgres-pv-storage
---
# Persistent Volume Claim
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-pv-claim
  namespace: hyuabot
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: postgres-pv-storage
---
# Deployment for database
apiVersion: apps/v1
kind: Deployment
metadata:
  name: hyuabot-database
  namespace: hyuabot
spec:
  replicas: 1
  selector:
    matchLabels:
      app: hyuabot-database
  template:
    metadata:
      labels:
        app: hyuabot-database
    spec:
      volumes:
      - name: postgres-pv-volume
        persistentVolumeClaim:
          claimName: postgres-pv-claim
      containers:
      - name: database
        image: postgres:17-alpine
        imagePullPolicy: IfNotPresent
        args:
        - "-c"
        - "shared_buffers=512MB"
        - "-c"
        - "effective_cache_size=2GB"
        ports:
        - containerPort: 5432
        env:
        - name: POSTGRES_USER
          valueFrom:
            secretKeyRef:
              name: secret
              key: DB_ID
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: secret
              key: DB_PASSWORD
        - name: PGDATA
          value: /var/lib/postgresql/data/pgdata
        - name: POSTGRES_DB
          value: hyuabot
        volumeMounts:
        - mountPath: /var/lib/postgresql/data
          name: postgres-pv-volume
        resources:
          requests:
            memory: "1Gi"
            cpu: "500m"
          limits:
            memory: "3Gi"
            cpu: "1500m"
---
# Service for database
apiVersion: v1
kind: Service
metadata:
  name: hyuabot-database
  namespace: hyuabot
  labels:
    app: hyuabot-database
spec:
  type: NodePort
  ports:
  - port: 5432
    nodePort: 30432
  selector:
    app: hyuabot-database
---
# Migration Job
apiVersion: batch/v1
kind: Job
metadata:
  name: migration-database
  namespace: hyuabot
spec:
  template:
    metadata:
      name: migration-database
    spec:
      initContainers:
        - name: wait-for-db
          image: busybox
          command: ['sh', '-c', 'until nc -z hyuabot-database 5432; do echo waiting for database; sleep 2; done;']
      containers:
      - name: migration-database
        image: postgres:17-alpine
        command: ["sh", "-c", "export PGPASSWORD=$POSTGRES_PASSWORD; psql -h hyuabot-database -U $POSTGRES_USER -d $POSTGRES_DB -f /scripts/create_database.sql"]
        volumeMounts:
        - name: migration-script
          mountPath: /scripts
          readOnly: true
        env:
        - name: POSTGRES_USER
          valueFrom:
            secretKeyRef:
              name: secret
              key: DB_ID
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: secret
              key: DB_PASSWORD
        - name: POSTGRES_DB
          value: hyuabot
      restartPolicy: Never
      volumes:
      - name: migration-script
        configMap:
          name: create-database
---
# Redis Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: hyuabot-redis
  namespace: hyuabot
spec:
  replicas: 1
  selector:
    matchLabels:
      app: hyuabot-redis
  template:
    metadata:
      labels:
        app: hyuabot-redis
    spec:
      containers:
      - name: redis
        image: redis:latest
        imagePullPolicy: IfNotPresent
        command: ["redis-server", "--maxmemory", "768mb", "--maxmemory-policy", "allkeys-lru"]
        ports:
        - containerPort: 6379
        resources:
          requests:
            memory: "256Mi"
            cpu: "125m"
          limits:
            memory: "1536Mi"
            cpu: "500m"
      # Sidecar exporting Redis server stats (keyspace hits/misses, memory, evictions) to Prometheus.
      - name: redis-exporter
        image: oliver006/redis_exporter:v1.62.0
        args: ["--redis.addr=redis://localhost:6379"]
        ports:
        - containerPort: 9121
          name: metrics
        resources:
          requests:
            memory: "32Mi"
            cpu: "25m"
          limits:
            memory: "64Mi"
            cpu: "100m"
---
# Service for Redis
apiVersion: v1
kind: Service
metadata:
  name: hyuabot-redis
  namespace: hyuabot
  labels:
    app: hyuabot-redis
spec:
  type: NodePort
  ports:
    - port: 6379
      nodePort: 30379
  selector:
    app: hyuabot-redis
---
# Service exposing the redis_exporter metrics endpoint for Prometheus to scrape.
apiVersion: v1
kind: Service
metadata:
  name: hyuabot-redis-exporter
  namespace: hyuabot
  labels:
    app: hyuabot-redis
spec:
  selector:
    app: hyuabot-redis
  ports:
    - name: metrics
      protocol: TCP
      port: 9121
      targetPort: 9121